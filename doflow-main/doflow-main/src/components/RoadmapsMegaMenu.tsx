import React from 'react';

interface RoadmapsMegaMenuProps {
  isOpen: boolean;
  onClose: () => void;
}

const RoadmapsMegaMenu: React.FC<RoadmapsMegaMenuProps> = () => {
  return null;
};

export default React.memo(RoadmapsMegaMenu);
